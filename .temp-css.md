# CSS 风格化待办清单

本文件仅记录本轮未执行的 CSS / 界面风格化问题，方便后续单独处理。

- `css/styles.filters.css`
  - 过滤器面板与下拉菜单仍有暗色硬编码颜色，需改为主题变量。
  - 下拉菜单隐藏态建议补 `pointer-events` 语义一致性。
  - 阴影颜色建议统一切到 token。

- `css/styles.layout.css`
  - `syncSlideDown` 关键帧命名建议改为 kebab-case。

- `css/styles.overlays.css`
  - `.selected-weapons-summary-text` 建议补单行截断。
  - `.notice-card` 建议从 `.about-card` 视觉体系拆出，改成接近 `sync-card` 的现代面板。
  - `.notice-body` / `.notice-footer` / `.notice-qq` 建议重做间距、边框和滚动职责划分。
  - 建议补移动端断点，优化窄屏布局与 safe-area 表现。

- `css/styles.recommendations.css`
  - `.coverage-badge` 建议显式补 `display: inline-flex` 与 `align-items: center`。

- `css/styles.theme.modes.css`
  - 亮色主题中存在重复选择器、残留孤儿样式和硬编码 alpha，建议统一清理。
  - `.notice-card` 建议从通用 `.about-card` 的亮色覆盖中拆出独立规则。
  - `.notice-footer` 的亮色专用覆盖在改为主题变量后可删除。

- `css/styles.weapons.css`
  - 需要统一 `.lock-chip` 与 `.lock-tag` 的样式兼容关系。
